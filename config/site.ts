export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Markdown Editor",
  description:
    "A simple Markdown editor with real-time preview and HTML export.",
  navItems: [],
  navMenuItems: [],
  links: {},
};
